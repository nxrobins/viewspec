{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://viewspec.dev/schemas/local-artifact-manifest-v1.json",
  "title": "ViewSpec Local Artifact Manifest v1",
  "type": "object",
  "required": [
    "version",
    "manifest_schema_version",
    "kind",
    "sdk_version",
    "source_name",
    "raw_source_hash",
    "source_hash",
    "design_hash",
    "artifact_hash",
    "command",
    "command_args",
    "policy_version",
    "guarantees",
    "nodes",
    "diagnostics",
    "external_refs"
  ],
  "properties": {
    "version": { "const": 1 },
    "manifest_schema_version": { "const": 1 },
    "kind": { "type": "string" },
    "sdk_version": { "type": "string" },
    "source_name": { "type": ["string", "null"] },
    "raw_source_hash": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
    "source_hash": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
    "design_hash": { "type": ["string", "null"], "pattern": "^[0-9a-f]{64}$" },
    "artifact_hash": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
    "command": { "type": "string" },
    "command_args": {
      "type": "array",
      "items": { "type": "string" }
    },
    "policy_version": { "type": "string" },
    "guarantees": {
      "type": "object",
      "required": ["sdk_network_calls", "artifact_autofetch_network", "decompilation"],
      "properties": {
        "sdk_network_calls": { "const": "none" },
        "artifact_autofetch_network": { "const": "none" },
        "network_calls": { "const": "none" },
        "decompilation": { "type": "string" }
      }
    },
    "nodes": { "type": "object" },
    "diagnostics": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["severity", "code", "message"],
        "properties": {
          "severity": { "enum": ["info", "warning", "error"] },
          "code": { "type": "string" },
          "message": { "type": "string" },
          "node_id": { "type": "string" },
          "path": { "type": "string" }
        }
      }
    },
    "external_refs": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["kind", "attr", "url", "behavior"],
        "properties": {
          "kind": { "type": "string" },
          "attr": { "type": "string" },
          "url": { "type": "string" },
          "behavior": { "type": "string" }
        }
      }
    }
  }
}
